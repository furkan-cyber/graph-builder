# Memory Facts

The memory adapter is not a standalone example script yet, but the package exports:

- `memoryFactToTextItem(...)`
- `createMemoryProvider(...)`
- `createMemoryExtractor()`

Use it when you want preference, fact, or workspace-memory style records to become graph nodes and edges.
