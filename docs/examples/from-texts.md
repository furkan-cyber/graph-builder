# From Texts

Command:

```sh
npm run examples:from-texts
```

This is the fastest way to understand the package. It builds a graph from two in-memory markdown records and prints graph stats, path output, and the generated report.
