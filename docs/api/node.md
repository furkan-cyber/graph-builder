# Node Entry

The `@cognipeer/graph-builder/node` entry is the filesystem-specific surface.

## Exports

- `createLocalFileProvider`
- `LocalFileDescriptor`
- `LocalFileProviderOptions`

Use this entry when you want the provider itself rather than the shorthand `graphBuilder("./path")` call.
